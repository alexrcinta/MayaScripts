import maya.cmds as mc


class Lights():
    
    LIGHT_PATH = 'D:/Scripts/DB/DB_DATA/LIGHTS/'

    #### OMNILIGHTS

    def OMNILIGHT_ORANGE_20m(self):
        
        LIGHT_NAME = 'OMNILIGHT_ORANGE_20m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_ORANGE_30m(self):
        
        LIGHT_NAME = 'OMNILIGHT_ORANGE_30m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_ORANGE_80m(self):
        
        LIGHT_NAME = 'OMNILIGHT_ORANGE_80m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_ORANGE_120m(self):
        
        LIGHT_NAME = 'OMNILIGHT_ORANGE_120m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_ORANGE_180m(self):
        
        LIGHT_NAME = 'OMNILIGHT_ORANGE_180m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_WHITE_20m(self):
        
        LIGHT_NAME = 'OMNILIGHT_WHITE_20m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_WHITE_30m(self):
        
        LIGHT_NAME = 'OMNILIGHT_WHITE_30m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_WHITE_80m(self):
        
        LIGHT_NAME = 'OMNILIGHT_WHITE_80m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_WHITE_120m(self):
        
        LIGHT_NAME = 'OMNILIGHT_WHITE_120m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def OMNILIGHT_WHITE_180m(self):
        
        LIGHT_NAME = 'OMNILIGHT_WHITE_180m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')


    #### SPOTLIGHTS

    def SPOTLIGHT_ORANGE_10m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_ORANGE_10m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_ORANGE_30m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_ORANGE_30m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_ORANGE_50m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_ORANGE_50m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_ORANGE_100m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_ORANGE_100m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_ORANGE_180m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_ORANGE_180m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_WHITE_10m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_WHITE_10m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_WHITE_30m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_WHITE_30m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_WHITE_50m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_WHITE_50m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_WHITE_100m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_WHITE_100m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')

    def SPOTLIGHT_WHITE_180m(self):
        
        LIGHT_NAME = 'SPOTLIGHT_WHITE_180m'
        
        mc.file(self.LIGHT_PATH + LIGHT_NAME + '.mb', i = True, type = 'mayaBinary', ignoreVersion = True, mergeNamespacesOnClash = False, options = 'v=0')
        mc.rename(LIGHT_NAME, LIGHT_NAME + '_001')
